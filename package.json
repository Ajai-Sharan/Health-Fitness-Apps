{
    "name": "my-app",
    "version": "1.0.0",
    "scripts": {
      "frontend:build": "cd frontend && npm run build",
      "frontend:start": "cd frontend && npm run start",
      "backend:build": "cd backend && npm run build",
      "backend:start": "cd backend && npm run start",
      "vercel:build": "npm run frontend:build && npm run backend:build",
      "vercel:start": "npm run frontend:start && npm run backend:start",
      "start": "npm run frontend:start && npm run backend:start"
    },
    "dependencies": {
      "axios": "^1.7.7",
      "bcrypt": "^5.1.1",
      "body-parser": "^1.20.3",
      "cors": "^2.8.5",
      "express": "^4.21.1",
      "express-session": "^1.18.1",
      "mongoose": "^8.7.1"
    }
  }